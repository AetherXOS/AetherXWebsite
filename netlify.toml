# Netlify Configuration File
# Modern React Router v7 & Serverless Functions Setup

[build]
  command = "yarn build"
  publish = "build/client"

[dev]
  command = "yarn dev"
  port = 8888
  targetPort = 3020

[functions]
  directory = "functions"
  node_bundler = "esbuild"
